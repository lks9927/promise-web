-- ======================================================================
-- [V6 웹훅 알람 수정]
-- 웹훅(send-alert Edge Function)이 업데이트 이전의 데이터(old_record)를 
-- 감지할 수 있도록 REPLICA IDENTITY를 FULL로 변경합니다.
-- 이 설정이 있어야 핑 횟수가 1 -> 2, 2 -> 3으로 올라갈 때 
-- 이전 핑 횟수와 비교하여 정상적으로 재알람(문자)이 발송됩니다.
-- ======================================================================

ALTER TABLE funeral_cases REPLICA IDENTITY FULL;
 